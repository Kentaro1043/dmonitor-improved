services:
  dmonitor:
    build: .
    image: dmonitor-improved:latest
    network_mode: host
    volumes:
      - dmonitor-rootfs:/var/lib/dmonitor/rootfs
      - dmonitor-cache:/var/cache/dmonitor
    restart: unless-stopped
    # ID-50などの実デバイスを使う場合は、ホスト側で/dev/dstarを用意してから有効化してください。
    # devices:
    #   - /dev/dstar:/dev/dstar

volumes:
  dmonitor-rootfs:
  dmonitor-cache:
